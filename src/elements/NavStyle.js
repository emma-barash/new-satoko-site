import styled from 'styled-components';

export const Nav = styled.div`
    text-align: center;
    /* padding: 10px; */
    /* margin-bottom: 20px; */
    width: 100vw;
    position: fixed;
    background-image: linear-gradient(#FFFFFF, darkgray, transparent);
`;