import styled from 'styled-components';

export const Nav = styled.div`
    border-radius: 20px;
    text-align: center;
    /* padding: 10px; */
    /* margin-bottom: 20px; */
    width: 100vw;
    position: fixed;
    background-image: linear-gradient(gray, darkgray, transparent);
`;